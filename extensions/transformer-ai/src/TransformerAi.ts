import * as vscode from 'vscode';


/**
 * Implements the VS Code Sheet Mutator interface for Excel files.
 * Supports reading and writing cells in Excel workbooks.
 */
export class AiSdk implements vscode.ChatParticipant {
	constructor() {
	}

}
